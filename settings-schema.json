{
    "menu-section": {
        "type": "header",
        "description": "Menu Settings"
    },

    "showCountry": {
        "type": "switch",
        "default": true,
        "description": "Show Exit Country"
    },

    "showCity": {
        "type": "switch",
        "default": true,
        "description": "Show Exit City"
    },

    "showIP": {
        "type": "switch",
        "default": true,
        "description": "Show Exit IP Address"
    },

    "mask-section": {
        "type": "header",
        "description": "Mask IP Address"
    },

    "mask-octet-1": {
        "type": "checkbox",
        "default": false,
        "description": "Hide 1st Octet"
    },

    "mask-octet-2": {
        "type": "checkbox",
        "default": false,
        "description": "Hide 2nd Octet"
    },

    "mask-octet-3": {
        "type": "checkbox",
        "default": false,
        "description": "Hide 3rd Octet"
    },

    "mask-octet-4": {
        "type": "checkbox",
        "default": false,
        "description": "Hide 4th Octet"
    },

    "tor-section": {
        "type": "header",
        "description": "Tor Settings"
    },

    "tor-host": {
        "type": "entry",
        "default": "127.0.0.1",
        "description": "Tor Host"
    },

    "socks-port": {
        "type": "spinbutton",
        "default": 9050,
        "min": 1,
        "max": 65535,
        "step": 1,
        "description": "SOCKS Port"
    },

    "control-port": {
        "type": "spinbutton",
        "default": 9051,
        "min": 1,
        "max": 65535,
        "step": 1,
        "description": "Control Port"
    },

    "refresh-interval": {
        "type": "spinbutton",
        "default": 15,
        "min": 5,
        "max": 300,
        "step": 5,
        "description": "Refresh Interval (seconds)"
    },

    "auto-proxy": {
        "type": "switch",
        "default": true,
        "description": "Enable Proxy when Tor starts"
    }
}
